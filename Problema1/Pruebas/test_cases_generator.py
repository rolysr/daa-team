def generate_test_cases(number_test_cases=1, max_height=20, max_cost_ops=20):
    """
        Method that generates random problem instance tuples
        with the format (n, hi, c, e, m, result)
    """